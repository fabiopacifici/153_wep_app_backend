{
  "name": "webapp_back",
  "version": "1.0.0",
  "description": "un’app di `libri` in cui si potranno lasciare `recensioni` pubbliche.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "node --watch --env-file=.env server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  }
}
